export interface FormData {
    name: string;
    email: string;
    phone: string;
    subject: string;
    message: string;
  }
  
export interface Errors {
    name: string;
    email: string;
    message: string;
 }